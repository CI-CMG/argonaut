argonaut:
  dac-directory: ''
  work-directory: ''
  dacs:
    - name: aoml
#      email:
#        - foo.bar@example.com
  index-cron: '0 * * * * ?'
  gdac-sync-cron: '30 3 * * * ?'
  float-merge-quiet-timeout: 'PT1H'
  file-checker-timeout: 'PT2M'


camel:
  springboot:
    main-run-controller: true

spring:
  threads:
    virtual:
      enabled: true

